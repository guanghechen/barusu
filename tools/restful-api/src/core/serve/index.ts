import { clearSchemaRootPath } from './clear'
import { ServeCommandContext } from './context'
import { serveDataSchema } from './serve-schema'


/***
 * Initialize a mock server project with templates
 * @param context
 */
export async function serve(context: ServeCommandContext): Promise<void> {

}
