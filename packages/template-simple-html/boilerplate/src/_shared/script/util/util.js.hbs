window.__util__ = (function util () {
  /**
   * 周期性执行任务
   * @param task 无参函数
   * @param interval 执行周期
   */
  async function doIntervalTask(task, interval, doImmediately) {
    if (doImmediately) {
      await task()
    }
    setTimeout(doIntervalTask, interval, true)
  }

  /**
   * 创建对 mock 数据的请求
   */
  function createRequester() {
    const CHART_DATA_URLS = {
    }

    // 异步请求工具类实例
    const requester = axios.create({
      baseURL: '/',
      timeout: 5000,
      headers: {

      }
    })

    return { requester, CHART_DATA_URLS }
  }

  return {
    doIntervalTask,
    createRequester,
  }
})()
